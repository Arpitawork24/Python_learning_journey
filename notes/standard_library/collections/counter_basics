# Counter is a tool in Python that helps you count how many times each item appears in a list, string, or other collection.

from collections import Counter

num = [1,2,1,3,4,2,5,2,1,5]
num_ct = Counter(num)
print(num_ct)

word = "banana"
word_ct = Counter(word)
print(word_ct)
